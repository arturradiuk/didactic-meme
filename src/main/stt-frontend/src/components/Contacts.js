import React from "react";

const Contacts = (props) => {
    return (
        <div style={{ margin: "80px" }}>
            <h1>Contacts Page</h1>
        </div>
    );
};

export default Contacts;
